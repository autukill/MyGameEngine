# BubbleTa 单功能模块

这里放置由 BubbleTa 真实玩法需求产生、但值得独立构建和无窗口验证的游戏局部模块。

模块默认不是 MyGameEngine 公共 Feature。只有出现第二个真实消费者，并且公共语义已经脱离 BubbleTa 规则后，才讨论向 `src/Engine.Features` 提升。

当前边界与依赖规则见 [项目结构与模块约定](../docs/PROJECT_STRUCTURE.md)。
